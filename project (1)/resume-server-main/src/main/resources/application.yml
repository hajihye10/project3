spring:
    application:
        name: resume
    server:
        port: 8080
        servlet:
            context-path: /
    datasource:
        url: jdbc:mysql://localhost:3306/resume
        username: root
        password: dnsgkdtlf11
        driver-class-name: com.mysql.cj.jdbc.Driver
    sql:
        init:
            mode: always
    thymeleaf:
        prefix: classpath:/templates/
        suffix: .html
        mode: HTML
        encoding: UTF-8
        cache: false

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.example.resume